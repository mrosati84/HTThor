# Build output and machine-local toolchain overrides.
build/
local.mk
*.o

# `odin test tests` drops its compiled runner in the repository root.
/tests.bin

# Scratch directory the test helpers fall back to when $TMPDIR is unset.
/tmp/

/.capture-sandbox/

/.opencode/
